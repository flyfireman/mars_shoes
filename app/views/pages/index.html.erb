<h2>内容管理系统</h2>
<ul>
  <% @pages.each do |page| %>
    <li>
      <%= link_to page.permalink, page_path(page) %>
      [<%= link_to '编辑',edit_page_path(page) %>]
      :<%= page.title %>
    </li>
  <% end %>
</ul>

